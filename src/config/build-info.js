(() => {
  // Visible only inside the administrator panel. Keep this file free of secrets.
  globalThis.LeagueBuildInfo = Object.freeze({
    release: "2026.09.23-item-settlement-preview",
    branch: "refactor/component-boundaries",
    itemSettlement: "legacy active · v2 staged",
  });
})();
